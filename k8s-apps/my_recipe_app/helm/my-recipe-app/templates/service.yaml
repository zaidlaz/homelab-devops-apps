apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-recipe-app.fullname" . }}-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "my-recipe-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "my-recipe-app.selectorLabels" . | nindent 4 }}
