apiVersion: v1
kind: Service
metadata:
  name: flask-app-portfolio-service
  namespace: flask-app-portfolio
spec:
  type: NodePort
  selector:
    app: flask-app-portfolio
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30081
